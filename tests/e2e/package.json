{
  "name": "@workspace/e2e",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:development-browser": "pnpm --filter @workspace/scripts run seed:ci && NODE_ENV=development PLAYWRIGHT_BASE_URL=\"${PLAYWRIGHT_BASE_URL:-https://${REPLIT_DEV_DOMAIN}}\" playwright test journeys/editorial-navigation.spec.ts journeys/planner-composition-reload.spec.ts",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "report": "playwright show-report playwright-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/node": "catalog:"
  }
}
