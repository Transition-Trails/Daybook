Goal: ZERO broken links via deterministic page ids from role+index only.
Page ids: cover(1); home(1, always present, tab target for Home-only mode); year(1); mdiv{0..n-1} month dividers; m{0..n-1} month calendars; w{ISOweek} weekly; d{YYYYMMDD} daily; todo(1); notes(1 index); ns{1..N} section dividers (N=sections.length, 0 allowed); notes-p{0..} paper (1 or 3 if mixed). Never reuse an id across roles. Month index follows the config range (startMonth/startYear/monthCount), spanning year boundaries.
Cross-links (all must resolve): cover->home; home->year,todo,notes,each ns{i}; year mini-month->mdiv{n}->m{n}; month day cell->d{date}; weekly<->its 7 d{date} and back to m{n}; daily->m{n} and prev/next d{date}; notes->each ns{i}; each ns{i}->notes; tab rail->each m{n}; when tabPos==none render ONLY a Home tab to home (no month rail).
Calendar links by output.calMode: google = https://calendar.google.com/calendar/render?action=TEMPLATE&dates=<start>/<end> (all-day weekly = YYYYMMDD/YYYYMMDD; timed daily = local datetime, length = eventMins); apple = generated .ics VEVENT (data:text/calendar URI or linked file); none = no links, plain ruled rows.
AI blocks: output.aiInPdf true renders Ask-AI prompt block on daily/weekly linking to user's assistant URL; false omits.
Art overlay: per-edition art.{role} Drive file ids drawn full-bleed behind generated text; link coordinates fixed by role so updated art drops in without moving links.
CI determinism test before every export: no duplicate ids; every internal href target exists; home+year always exist; count(ns*)==sections.length; calendar URLs parse.
